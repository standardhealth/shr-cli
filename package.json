{
  "name": "shr2js",
  "version": "1.0.0",
  "description": "Converts Standard Health Record (SHR) elements from their custom grammar to JSON",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec --recursive",
    "test:watch": "npm test -- --watch"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "antlr4": "^4.5.3"
  },
  "devDependencies": {
    "chai": "*",
    "mocha": "*"
  }
}
